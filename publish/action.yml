name: Publish
description: Configure npm registry and publish package

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 24
        registry-url: https://registry.npmjs.org

    - run: npm publish --access public
      shell: bash
